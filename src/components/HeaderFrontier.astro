---
import "../styles/fonts.css";
import "../styles/colors.css";
import "../styles/transition.css";
import { Icon } from "astro-icon/components";

// Logo
import LogoSVG from "../../public/lublio-logo-dark.svg?raw";
---

<header id="main-header" class="header">
  <div class="logo">
    <a id="menu-open-btn">
      <Fragment set:html={LogoSVG} />
    </a>
  </div>
</header>

<div id="overlay-menu" class="overlay">
  <!-- Panels -->
  <div class="overlay-panel panel-1"></div>
  <div class="overlay-panel panel-2"></div>
  <div class="overlay-panel panel-3"></div>
  <div class="overlay-panel panel-4"></div>
  <div class="overlay-panel panel-5"></div>
  <div class="overlay-panel panel-6"></div>
  <div class="overlay-panel panel-7"></div>
  <div class="overlay-panel panel-8"></div>
  <div class="overlay-panel panel-9"></div>
  <div class="overlay-panel panel-10"></div>

  <div class="overlayContent">
    <a id="menu-close-btn" class="close">&times;</a>
    <ul class="menuList">
      <li>
        <a href="/" class="menuLink">Home</a>
      </li>
      <li>
        <a href="/franco" class="menuLink">Franco</a>
      </li>
      <li>
        <a href="/russo" class="menuLink">Russo</a>
      </li>
      <!-- <li>
        <a href="/about" class="menuLink">About</a>
      </li> -->
    </ul>
  </div>
</div>

<script>
  // Overlay Menu 로직
  const overlay = document.getElementById("overlay-menu");
  const openBtn = document.getElementById("menu-open-btn");
  const closeBtn = document.getElementById("menu-close-btn");

  openBtn?.addEventListener("click", () => {
    overlay?.classList.add("overlayActive"); // styles.overlayActive 역할
  });

  closeBtn?.addEventListener("click", () => {
    overlay?.classList.remove("overlayActive");
  });
</script>

<style>
  .header {
    position: fixed;
    top: 0;
    justify-self: right;
    width: auto;
    max-width: 768px;
    margin: 0 auto;
    height: 40px;
    z-index: 99;
    transition: all 0.4s;
    opacity: 0;
    background-color: transparent;
    transform: translateY(-50px);
  }

  .logo {
    position: relative;
    width: 40px; /* 원하는 크기 조절 */
    height: auto;
    margin-left: auto;
    padding: 0.5rem;
    transition: 0.4s all;
  }

  .logo > a {
    width: 100%;
    height: 100%;
  }

  .logo :global(svg) {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
  }

  .sticky {
    border: 1px solid rgba(255, 255, 255, 0.3);
    background-color: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(2px);
    margin-top: 1.5rem;
    padding: 0;
    opacity: 1;
    transform: translateY(0px);
  }

  .sticky:hover {
    cursor: pointer;
    background-color: rgba(255, 255, 255, 0.2);
  }

  .overlay {
    position: fixed;
    display: flex;
    flex-direction: column;
    justify-content: end;
    opacity: 1;
    height: 100vh;
    width: 0%;
    z-index: 999;
    right: 0px;
    top: 50%;
    margin: 0;
    transform: translateY(-50%);
    background-color: transparent;
    text-transform: uppercase;
    transition: width 0s linear 0.9s;
  }

  .overlayActive {
    opacity: 1;
    width: 100%;
    transition: width 0s;
  }

  .overlay-panel {
    position: absolute;
    top: 0;
    right: 0;
    width: 0%;
    height: 100%;
    transition: width 1s cubic-bezier(0.77, 0, 0.175, 1);
  }

  /* Gradation Style */
  .panel-1 {
    background-color: rgba(23, 23, 23, 0.1);
    transition-delay: 0s;
  }
  .panel-2 {
    background-color: rgba(23, 23, 23, 0.2);
    transition-delay: 0.1s;
  }
  .panel-3 {
    background-color: rgba(23, 23, 23, 0.3);
    transition-delay: 0.2s;
  }
  .panel-4 {
    background-color: rgba(23, 23, 23, 0.4);
    transition-delay: 0.3s;
  }
  .panel-5 {
    background-color: rgba(23, 23, 23, 0.5);
    transition-delay: 0.4s;
  }
  .panel-6 {
    background-color: rgba(23, 23, 23, 0.6);
    transition-delay: 0.5s;
  }
  .panel-7 {
    background-color: rgba(23, 23, 23, 0.7);
    transition-delay: 0.6s;
  }
  .panel-8 {
    background-color: rgba(23, 23, 23, 0.8);
    transition-delay: 0.7s;
  }
  .panel-9 {
    background-color: rgba(23, 23, 23, 0.9);
    transition-delay: 0.8s;
  }
  .panel-10 {
    background-color: rgba(23, 23, 23, 1);
    transition-delay: 0.9s;
  }

  .overlayActive .overlay-panel {
    width: 100%;
  }

  .overlay:not(.overlayActive) .panel-1 {
    transition-delay: 0.9s;
  }
  .overlay:not(.overlayActive) .panel-2 {
    transition-delay: 0.8s;
  }
  .overlay:not(.overlayActive) .panel-3 {
    transition-delay: 0.7s;
  }
  .overlay:not(.overlayActive) .panel-4 {
    transition-delay: 0.6s;
  }
  .overlay:not(.overlayActive) .panel-5 {
    transition-delay: 0.5s;
  }
  .overlay:not(.overlayActive) .panel-6 {
    transition-delay: 0.4s;
  }
  .overlay:not(.overlayActive) .panel-7 {
    transition-delay: 0.3s;
  }
  .overlay:not(.overlayActive) .panel-8 {
    transition-delay: 0.2s;
  }
  .overlay:not(.overlayActive) .panel-9 {
    transition-delay: 0.1s;
  }
  .overlay:not(.overlayActive) .panel-10 {
    transition-delay: 0s;
  }

  .overlayActive .overlayContent {
    opacity: 1;
    transition: opacity 1s ease 1s; /* 패널이 다 덮인 후 텍스트 등장 */
  }

  .close {
    display: none;
    opacity: 0;
    width: auto;
    height: auto;
    text-align: center;
    z-index: 999;
    transition: all 1s ease 1s;
  }

  .overlayActive .close {
    display: block;
    position: absolute;
    opacity: 1;
    background-color: transparent;
    top: -6rem;
    left: 50%;
    transform: translateX(-50%);
    font-size: 50px;
    font-family: "Inter", sans-serif;
    font-weight: 300;
    text-decoration: none;
    color: var(--neutral50);
    cursor: pointer;
    transition: all 0.7s ease 0.7s;
  }

  .overlayActive .close:hover {
    cursor: pointer;
    color: var(--neutral400);
  }

  .overlayContent {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    height: auto;
    width: 100%;
    flex-direction: column;
    align-items: center;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
    opacity: 0;
  }

  .menuList {
    opacity: 0;
    list-style: none;
    transform: translateX(200px);
    background-color: transparent;
  }

  .overlayActive .menuList {
    opacity: 1;
    width: 100%;
    max-width: 280px;
    height: 100%;
    margin: 0;
    transform: translateX(0px);
  }

  .menuList li {
    display: block;
    text-align: left;
    width: auto;
    margin-bottom: 0rem;
    transition: all 0.4s ease-in-out;
  }

  .menuList li:last-child {
    margin-bottom: 0;
  }

  .menuLink {
    display: inline-block;
    width: 100%;
    height: 100%;
    position: relative;
    margin: 0px;
    margin-bottom: 1rem;
    text-decoration: none;
    font-family: "Inter", sans-serif;
    font-size: 24px;
    font-weight: 600;
    color: var(--neutral50);
    padding: 1.5rem 2rem;
    background-color: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.4s;
  }

  .menuLink:hover {
    background-color: rgba(255, 255, 255, 0.2);
    cursor: pointer;
  }

  .menuLink > svg {
    width: 30px;
    margin-right: 0.5rem;
    color: var(--zinc800);
    transform: translateY(6px);
    transition: all 0.4s;
  }

  /* Mobile */
  @media (max-width: 480px) {
    .overlayActive .menuList {
      max-width: 240px;
    }

    .menuLink {
      font-size: 24px;
    }
  }
</style>

<script>
  // 클라이언트 측 로직 (스크롤 및 메뉴 토글)
  const header = document.getElementById("main-header");
  const menuTrigger = document.getElementById("menu-trigger");
  const menuClose = document.getElementById("menu-close");
  const overlay = document.getElementById("overlay-menu");

  // 1. Sticky Header 로직
  window.addEventListener(
    "scroll",
    () => {
      if (window.scrollY > 0) {
        header?.classList.add("sticky");
      } else {
        header?.classList.remove("sticky");
      }
    },
    { passive: true },
  );

  // 2. 오버레이 메뉴 제어
  menuTrigger?.addEventListener("click", () => {
    overlay?.classList.add("overlayActive");
  });

  menuClose?.addEventListener("click", () => {
    overlay?.classList.remove("overlayActive");
  });
</script>
