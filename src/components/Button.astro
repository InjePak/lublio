---
import "../styles/fonts.css";
import "../styles/colors.css";
import "../styles/transition.css";

const { title, subtitle, description, language, path } = Astro.props;
const colorMap: Record<string, string> = {
  eng: "var(--pink400)",
  fra: "var(--sky400)",
  deu: "var(--amber400)",
  rus: "var(--indigo400)",
  kaz: "var(--cyan400)",
};
const borderColor = colorMap[language] || "var(--zinc300)";
---

<style define:vars={{ borderColor }}>
  .btn:hover {
    border: 2px solid var(--borderColor);
  }
</style>

<a href={path}>
  <div class="btn">
    <h1>{title}</h1>
    <h2>{subtitle}</h2>
    <p>{description}</p>
  </div>
</a>

<style>
  .btn {
    position: relative;
    border: 2px dashed var(--zinc800);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    min-height: 300px;
    transition: all 0.4s;
  }

  .btn > h1 {
    position: relative;
    font-size: 32px;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .btn > h2 {
    position: relative;
    font-size: 20px;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .btn > p {
    position: absolute;
    left: 1.5rem;
    bottom: 0;
  }

  .btn:hover {
    border-radius: 15px;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .btn > h1 {
      font-size: 22px;
    }

    .btn > h2 {
      font-size: 15px;
    }

    .btn > p {
      font-size: 14px;
    }
  }
</style>
